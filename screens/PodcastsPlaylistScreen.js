import React from "react";
import { StyleSheet, Text, View } from "react-native";

const PodcastsPlaylistScreen = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default PodcastsPlaylistScreen;

const styles = StyleSheet.create({});
